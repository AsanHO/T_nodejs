extends base.pug
include mixins/video

block content 
    form(method="GET")
        input(placeholder="Search by text",name="keyword",type="text")
        input(type="submit", value="🔍")
    
    each video in videos 
        +video(video)