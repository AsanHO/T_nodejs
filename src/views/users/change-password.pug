extends ../base

block content
    if errorMessage
        span=errorMessage
    form(method = "POST")
        input(name ="oldPW",placeholder="old password", type="password")
        input(name ="newPW",placeholder="new password", type="password")
        input(name ="newPWConfirm",placeholder="new password confirm", type="password")
        input(value="Save", type="submit")